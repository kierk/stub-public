<%= form_for(@stub) do |f| %>
  <% if @stub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stub.errors.count, "error") %> prohibited this stub from being saved:</h2>

      <ul>
      <% @stub.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
